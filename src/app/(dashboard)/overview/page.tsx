async function page() {
  // const supabase = createClient();
  // const {
  //   data: todos,
  //   error,
  //   count,
  // } = await supabase.from('todos').select('*', { count: 'exact' });

  // console.log(count);
  // console.log(todos);
  // console.log(error);
  return <div>page</div>;
}

export default page;
